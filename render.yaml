services:
  - type: web
    name: intellaiq-engine
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python engine_server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: TYPESAFE_API_KEY
        sync: false